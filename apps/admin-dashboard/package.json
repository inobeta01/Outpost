{
  "name": "@outpost/dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "Operator dashboard — visualize registry contents, staleness, confidence distributions. Placeholder.",
  "scripts": {
    "typecheck": "echo 'no TS yet — placeholder' && exit 0"
  }
}
